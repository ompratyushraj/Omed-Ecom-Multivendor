import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#00a152',
    },
    secondary: {
      main: '#00a152',
    },
  },
});

export default theme;
